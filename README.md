### 在 `powerShell`/`cmd` 执行以下命令
### **😈打开本地文件**
``` bash
cd "D:\hexo-blog"
```
### **👽新建文章页**
``` bash
hexo new post name
``` 
### **😝同步至GitHub**
``` bash
hexo g -d
```
<br>

### **⚠️warning⚠️**
若你的文件放在D盘目录下,而cmd是 
```
C:\Users\...>
```
则需执行
```
cd "D:\hexo-blog"
```
或先切换到D盘目录